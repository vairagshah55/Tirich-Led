# https://www.robotstxt.org/robotstxt.html
User-agent: *
Allow: /

# Private / utility routes. Nothing links to them and they also emit
# <meta name="robots" content="noindex,nofollow">.
Disallow: /login
Disallow: /dashboard
Disallow: /ai-studio

# Deliberately NOT disallowed:
#   /products?search=…   — sends noindex,follow and canonicalises to /products/
#   /products?category=… — canonicalises to /products/category/<slug>/
# Blocking either would stop crawlers from ever reading those directives, so the
# duplicates would stay in the index unconsolidated.
#
# The lead-inbox route is deliberately absent too: robots.txt is public, so
# listing an unguessable path here would publish it. That page sends
# noindex,nofollow and is linked from nowhere.

Sitemap: https://tirichled.com/sitemap.xml

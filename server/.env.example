PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# CORS — frontend origin
CLIENT_ORIGIN=http://localhost:3000

# Database (add your DB url when ready)
# DATABASE_URL=
